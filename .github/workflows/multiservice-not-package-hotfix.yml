name: Multiservice is not package hotfix

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/multiservice_not_package_patch.py'
      - '.github/workflows/multiservice-not-package-hotfix.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Apply multiservice package classification patch
        run: python .github/scripts/multiservice_not_package_patch.py

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck diagnostics
        run: bunx tsc --noEmit

      - name: Production build
        run: bun run build

      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo "No source changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/admin-appointments-workspace.tsx src/components/finance-attendance-completion.tsx
          git commit -m "fix: do not treat multiple services as a package"
          git push
